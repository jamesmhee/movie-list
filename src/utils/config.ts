export const config = {
    api: {
        url: process.env.NEXT_PUBLIC_API_URL,
        apiKey: process.env.NEXT_PUBLIC_API_READ_KEY,
    },
    url: {
        img: process.env.NEXT_PUBLIC_IMG_URL,
    },
}
